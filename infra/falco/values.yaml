falco:
  grafana:
    dashboards:
      enabled: true
      configMaps:
        falco:
          namespace: prometheus-stack
  serviceMonitor:
    create: true
    labels:
      release: kube-prometheus-stack
